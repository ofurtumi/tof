#include <stdio.h>
#include <string.h>

#define MAX_LEN 200
int main(int argc, char **argv)
{
    char innstr[MAX_LEN];
    printf("%d", innstr[1]);
    while (scanf("%s", innstr) != EOF)
    {
        
    }
    
    return 0;
}